import defaultCollection from './defaultCollection';

const collections = [ defaultCollection ]; // TODO: Add more default collection templates by category (news / media / notes / art / music / etc)

export default collections;